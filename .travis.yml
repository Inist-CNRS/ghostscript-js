language: node_js
node_js:
  - "10"
  - "12"

env:
  - CXX=g++-4.8

sudo: true

before_install:
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo apt-get update -q
  - sudo apt-get install -y libgs-dev g++ cmake
